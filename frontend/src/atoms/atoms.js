import {atom} from 'recoil'

export const locationPanelOpenAtom = atom({
   key:'locationPanelOpenAtom',
   default: false
});

export const vehicelPanelAtom = atom({
    key:'vehiclePanelAtom',
    default: false
})